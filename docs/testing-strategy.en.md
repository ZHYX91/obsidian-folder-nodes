---
source_language: zh-CN
translation_of: testing-strategy.zh-CN.md
translation_status: synced
---

# Folder Nodes — Testing strategy

## Automated gates

`npm run check` pins Node/npm, then runs lint, formatting, bilingual-document contracts, strict TypeScript, coverage, production bundle, and release-layout checks. Core, Settings, critical structural adapters, and the production Node Graph entry/state/index/renderer path use thresholds of statements 80%, lines 80%, functions 75%, and branches 70%; existing thresholds are not lowered to admit the graph. Tests cover paths plus Windows reserved names and grapheme-safe truncation, selection naming, safe table-cell aliases, cross-cell rejection, unresolved-link target and display-alias planning, Visuals, malformed frontmatter boundaries, non-Markdown bulk-organization collisions, exact-path/name-start unmanaged rules, protected system folders, deep settings normalization, curated Emoji-font validation and fallback stacks, the canonical/legacy property matrix, sparse ordering, and incremental reverse references. NodeService uses an in-memory Vault/FileManager behavior fixture covering serialized creates, transactional explicit-path Node creation and rollback, link-safe rename/move, target-rank reconciliation after native cross-parent moves, note-only move/delete, synchronized folder/canonical-note rename, folder-side and Markdown-side incomplete nodes, continuous classification without initialization, ignored-subtree moves, bulk-organization and property-migration TOCTOU/post-commit validation, merge conflicts, rollbacks, and lifecycle abort after an in-flight write. Synchronous and asynchronous structural scanners must produce equivalent plans, and cancellation must perform no write. VaultOperationCoordinator tests lock serialization after failure, recursive event attribution, and TTL. UI/runtime tests lock Explorer-surface start/stop cleanup, Root eye state/click separation, no mobile draggable ownership, retained native create controls plus New node, incomplete/unmanaged/conflict states, the grouped action panel, note-title icons outside editable title text, measured title-icon alignment, visual-kind font classes, local Emoji-font detection failures, cross-document runtime font updates, disclosure selectors, before/into/after zones, drag payload, Shift+F10/Menu keys, and the settings hierarchy/explanation cards; source architecture contracts prohibit a global body observer, `vault.rename`, and Contents-wide `resolvedLinks` scans.

Node Graph tests import the real production entry and exercise registration, enable/disable, leaf reuse/close, state migration, and teardown rather than relying on source-string assertions. Pure behavior fixtures cover all three progressive defaults, root/local boundaries, multiple simultaneous branches, direct/Alt/range expansion, collapse to level 1, session-only expansion, old relation migration, search snapshot restoration, external focus, node-body open/menu behavior, Tooltip/keyboard/ARIA, Visual fallback, left-to-right/top-to-bottom structure anchors, independent link anchors, structure edges surviving link LOD, and one RuntimeStyles authority. Narrow-screen, coarse-pointer, and synthetic touch coverage protects Android-compatible code without being represented as real Android host evidence.

Regressions additionally inject a concurrent closed-file edit, an unsaved open editor, a same-path TFile replacement, and a same-path folder replacement during rollback. Settings-save tests delay and fail earlier persistence calls to prove snapshot isolation, ordering, and recovery of the queued latest state.

## Performance

Automation locks canonical `folder-nodes` token parsing/writing/deletion, published legacy reads, equivalent dual values, fail-closed conflicts/invalid/duplicate YAML, source-preserving explicit migration, stale-preview rejection, descendant inheritance, Root/incomplete/unmanaged write rejection, unmanaged and master-switch precedence, merge non-copying, Explorer/Contents/Graph projections, Root-eye session reveal, and graph metadata invalidation. Isolated-Vault acceptance also covers both languages, no hover dependency on mobile, a Hidden text badge on an explicit hidden root, muted inherited-source feedback, master-off preserving YAML, reload resetting the session switch, and no effect on Obsidian Search, Quick Switcher, backlinks, native Graph, or direct access.

The quick gate covers 10,000 and the large gate 100,000 direct Child Nodes; a normal reorder plan must finish within two seconds and create one property patch. Local rebalance for crowded ranks is bounded to 64 nodes. The reverse-reference benchmark builds quick 20,000/large 100,000-source indexes and applies 1,000 incremental removals. The incomplete-node scan checks near-linear planning and exact counts for quick 20,000/large 100,000 entries. RefreshScheduler tests combine 10,000 requests into one batch. In one deterministic serial worker, the Node Graph benchmark builds the real production GraphIndex for quick 20,000/large 100,000-node fixtures, then applies 32/128 non-nested dirty roots in one incremental refresh. Quick initial build and refresh each have a 2-second threshold; their large equivalents each have a 6-second threshold. Search-candidate aggregation plus an exact query has 1-second quick and 3-second large thresholds. The same refreshed snapshot is fully expanded through topology, VisibleScene, model aggregation, and 2D layout, with 4-second quick and 6-second large thresholds; pure 3D layout plus one 1920×1080 projection has 2-second quick and 5-second large thresholds. These deterministic computation gates expose linear regressions but do not prove interactive Canvas frame rate or host UI responsiveness. Separate GraphIndex behavior tests lock one initial inventory, path-targeted subtree refresh, sibling order, link-only invalidation, and exact scan counters. Those counters prove that scope, search, expansion, dimension, and **Show links** do not repeat a Vault-wide inventory. Full expansion keeps DOM node counts constant, preserves all visible structure edges, and applies the configured limit only to link edges. Contents View paginates Nodes, Album, and Files independently at 200 entries per batch. Album images draw once from resource URIs to canvas, and ordinary files do not read binary bodies.

## Isolated-Vault host acceptance

Only a disposable isolated Vault may validate plugin loading, all five settings pages (General, Management, Icons & appearance, Selection & naming, Node Graph), Follow Obsidian/English/Chinese, settings that begin with one scrollable tab row and do not repeat the plugin or active-tab heading, incomplete nodes appearing without initialization, cancellable bulk/property previews, strictly read-only combined Health, two unified unmanaged-content groups with `.`/`_` name-start defaults and natural-language rule rows, homepage command/button/restart, the pinned non-collapsible Root row with the only session eye control, retained native New note/New folder controls plus New node, before/after/hidden Explorer icons and title icons, visible badge separation between property icons and filename-leading characters, glyph/CJK/emoji font fallback, the installed-only Emoji font list, complex-sequence preview, unavailable-font fallback and manual re-detection, title icons staying aligned and outside node-name text/cursor/copy, independent main-window and popout Explorers, complete DOM/listener/order restoration on disable, canonical-note hiding, and trailing text badges using purple Hidden, gray Unmanaged, orange Incomplete, and red Conflict while inherited hidden descendants are only muted with source feedback, short first-level menus plus the grouped Folder Nodes action panel, disabled reorder boundaries, explicit subtree wording, hover/focus primary actions, badges with no double-click behavior, the disclosure-arrow/folder-title click boundary, Explorer and Contents before/into/after Node drag, native folder/note menu preservation, all three Contents menus and Shift+F10/Menu keys, single-file drops onto a Node/header/breadcrumb, multi-selected link drags causing no partial moves, name collisions and descendant rejection, dragend/Escape cleanup, selection context preview/body write/source-wikilink replacement, `\|` table-cell aliases, cross-cell fail-closed behavior, aliases and basename, the naming explanation card, `[[a]]` and `[[a|b]]` direct creation with aliases both enabled and disabled, explicit paths and missing ancestors, modifier-click panes, popout documents, conflict rollback, ignored/exempt fallthrough, native create/delete changing only the side that exists with no automatic completion or recreation, explicit leaf-Markdown conversion, explicit incomplete-folder completion, Set as unmanaged and Manage again, real conflict classification/repair, Visual Picker complete-list loading/add/remove/reorder/presets/two previews/inheritance/missing-image fallback/color accent/invalid-value rejection, nodes without visuals, static Album, GIF stills, video tiles, compact audio/HEIC files, independent pagination, narrow sidebar, merge conflicts, and system-trash deletion. The migration fixture must cover legacy-only, equivalent dual, conflicting dual, canonical-only, invalid token, non-canonical placement, CRLF/BOM, unsaved editor, and stale preview. Acceptance must confirm no `<video>`, `<audio>`, or autoplay. Automated tests do not prove those host behaviors.

The same exact candidate must exercise Node Graph Global/Subtree/Local progressive defaults, Local's parent context without sibling escape, multiple branches, direct/Alt/range expansion, collapse to level 1, link-off default and link-on overlay, relation-state migration, property-hidden subtree pruning with no independent include/exclude settings, SearchComponent reveal/restore, external focus, node menu and canonical-note opening, 2D/3D shared selection, readable zoom, large-scene constant DOM, absence of graph-covering density notices, narrow-window wrapping, Visual fallback, keyboard controls, and clean close/disable. Expansion is checked within a session and must return to one level after an Obsidian restart.

## Themes and accessibility

Check at least default light, default dark, and one third-party theme. Keyboard checks cover all five settings tabs, modal/action-panel buttons, Contents cards/rows, More actions, context-menu keys, breadcrumbs, the Root eye, and the Node Graph visual/body/child-handle controls. Drag has equivalent menu move/reorder actions, and coarse-pointer targets are 44px. English and Chinese must not truncate critical buttons or node titles. Follow Obsidian must match Obsidian's current interface language. Images use empty decorative alt text or filename alt text. The Root row is checked for one idempotent insertion, no disclosure control, separate keyboard activation for Root and eye, accurate pressed state, and active state. Explorer and title icons are checked for Incomplete node, Unmanaged, and Conflict badge contrast, glyph/CJK/emoji font fallback, size, vertical and horizontal alignment, and name spacing at multiple UI scales. Node Graph additionally checks tooltips, visible focus, explicit accessible names, dashed-link contrast, left-to-right/top-to-bottom handle rotation, and toolbar wrapping without covering the graph.

## Android mobile acceptance

Every `isDesktopOnly: false` candidate must retain Android-compatible code and CSS, with automated narrow-screen, coarse-pointer, and touch coverage. Release closure also requires the current Android 15/API 35 emulator and a disposable Vault to cover plugin load, File Explorer/Contents, native moves, menus, settings persistence, Node Graph scopes/handles/search/2D/3D, narrow layout, touch targets, system trash, and disable cleanup. Android physical devices and iOS are out of scope.

## Production deployment

A production Vault requires explicit authorization for its exact path. Confirm Obsidian is stopped, preserve `data.json`, replace only `main.js`, `manifest.json`, and `styles.css`, and compute SHA-256 for candidate and deployed files. Production deployment is not acceptance; the user's manual acceptance result is recorded separately.
